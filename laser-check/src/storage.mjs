let promise;
function db(){return promise??=new Promise((resolve,reject)=>{const request=indexedDB.open('valida-laser',1);request.onupgradeneeded=()=>{const table=request.result.createObjectStore('comparisons',{keyPath:'id'});table.createIndex('date','date');};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error);request.onblocked=()=>reject(new Error('Feche outras abas do aplicativo e tente novamente.'));});}
export async function saveRecord(record){const database=await db();await new Promise((resolve,reject)=>{const tx=database.transaction('comparisons','readwrite');tx.objectStore('comparisons').add(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error);});}
export async function listRecords(){const database=await db();return new Promise((resolve,reject)=>{const request=database.transaction('comparisons','readonly').objectStore('comparisons').getAll();request.onsuccess=()=>resolve(request.result.sort((a,b)=>b.date.localeCompare(a.date)));request.onerror=()=>reject(request.error);});}
